# Roadmap

- [x] Answer: does the database have a table named "subscriptons" / "subscriptions"?
- [ ] Start this project's email-sending update (consent given via the Lovable prompt) — call start_email_migration, relay its message, then stop; the code rewrite happens in a later funded turn.
